#!/bin/bash

wget https://sliver.sh/install -O /tmp/sliver.sh
chmod +x /tmp/sliver.sh

apt-get update
/tmp/sliver.sh
apt-get clean
rm -rf /var/lib/apt/lists/*

rm -rf /tmp/sliver.sh

service stop sliver
systemctl enable sliver
